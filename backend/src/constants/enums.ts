export enum UserVerifyStatus {
  Unverified,
  Verified
}

export enum TokenType {
  AccesssToken,
  RefreshToken,
  EmailVerifyToken,
  ForgotPasswordToken
}

export enum Role {
  User,
  Admin
}

export enum OTPVerifyStatus {
  Unverified,
  Verified
}
